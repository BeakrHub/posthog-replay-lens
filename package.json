{
  "name": "replay_lens",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "description": "Local-first session replay analysis with Gemini video understanding.",
  "license": "MIT",
  "keywords": [
    "posthog",
    "session-replay",
    "gemini",
    "ux-research",
    "bug-reporting"
  ],
  "engines": {
    "node": ">=20.19"
  },
  "scripts": {
    "dev": "concurrently \"npm:server\" \"npm:web\"",
    "start": "node server/index.mjs",
    "server": "node server/index.mjs",
    "web": "vite --host 127.0.0.1",
    "analyze": "node scripts/run-once.mjs",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1",
    "check": "node --check server/index.mjs && node --check server/posthog.mjs && node --check server/replay.mjs && node --check server/gemini.mjs && node --check server/job-runner.mjs && node --check server/automation.mjs && node --check scripts/run-once.mjs && vite build"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^5.1.1",
    "concurrently": "^9.2.1",
    "express": "^5.2.1",
    "lucide-react": "^0.562.0",
    "playwright": "^1.57.0",
    "react": "^19.2.3",
    "react-dom": "^19.2.3",
    "rrweb": "^2.0.0-alpha.21"
  },
  "devDependencies": {
    "vite": "^8.0.16"
  }
}
